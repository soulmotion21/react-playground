import {create} from '@storybook/theming';
import logo from '../src/main/webapp/app/asset/img/logo.png'

export default create({
  base: 'light',
  brandTitle: 'Planit Healthcare',
  brandUrl: '/',
  brandImage: logo,
});
